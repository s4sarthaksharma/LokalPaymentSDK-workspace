<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Proxy Activity that fronts Razorpay's UPI Intent flow. Merged into the
         host app so the SDK is self-contained: consumers don't register anything. -->
    <application>
        <activity
            android:name="com.getlokalapp.paymentsdk.razorpay.RazorpayUpiIntentActivity"
            android:exported="false"
            android:configChanges="orientation|keyboardHidden|screenSize|screenLayout|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Auto-init: registers RazorpayUpiIntentSdk with LokalPaymentSdk at
             process start, zero host code (see the provider's kdoc). -->
        <provider
            android:name="com.getlokalapp.paymentsdk.razorpay.RazorpayUpiIntentInitProvider"
            android:authorities="${applicationId}.getlokalapp.paymentsdk.razorpay.upiintent.initprovider"
            android:exported="false" />
    </application>

</manifest>
