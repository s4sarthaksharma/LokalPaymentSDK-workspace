<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Proxy Activity that fronts Razorpay Checkout. Merged into the host app
         so the SDK is self-contained: consumers don't register anything. -->
    <application>
        <activity
            android:name="com.getlokalapp.paymentsdk.razorpay.RazorpayCheckoutActivity"
            android:exported="false"
            android:configChanges="orientation|keyboardHidden|screenSize|screenLayout|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Auto-init: registers RazorpayCheckoutSdk with LokalPaymentSdk at
             process start, zero host code (see the initializer's kdoc).
             Contributed to App Startup's single provider as a <meta-data>. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.getlokalapp.paymentsdk.razorpay.RazorpayCheckoutInitializer"
                android:value="androidx.startup" />
        </provider>
    </application>

</manifest>
